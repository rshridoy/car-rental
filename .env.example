# Vertex AI Proxy — shared secret
# Required by src/lib/ai/gemini.ts for the AI chat / recommendation feature.
# Get this value from your GCP project secret manager or the project owner.
# Never commit the real secret to source control.
PROXY_SECRET=
